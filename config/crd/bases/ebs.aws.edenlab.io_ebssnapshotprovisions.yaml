---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: ebssnapshotprovisions.ebs.aws.edenlab.io
spec:
  group: ebs.aws.edenlab.io
  names:
    kind: EBSSnapshotProvision
    listKind: EBSSnapshotProvisionList
    plural: ebssnapshotprovisions
    singular: ebssnapshotprovision
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: PHASE
      type: string
    - jsonPath: .status.snapshotsFound
      name: SNAPSHOTS-FOUND
      type: integer
    - jsonPath: .status.lastUpdated
      name: LAST-UPDATED
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EBSSnapshotProvision is the Schema for the ebssnapshotprovisions
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of EBSSnapshotProvision
            properties:
              clusterName:
                description: |-
                  ClusterName is the name of the Kubernetes cluster whose EBS snapshots
                  should be provisioned. It is used to filter AWS snapshots by the
                  "kubernetes.io/cluster/<ClusterName>" tag.
                type: string
              frequency:
                description: |-
                  Frequency defines how often the controller should reconcile and check
                  for new AWS snapshots to provision.
                type: string
              region:
                description: Region is the AWS region to look up EBS snapshots in.
                type: string
              volumeSnapshotClassName:
                description: |-
                  VolumeSnapshotClassName is the name of the VolumeSnapshotClass used
                  when creating VolumeSnapshot and VolumeSnapshotContent resources.
                type: string
              waitForReadyToUse:
                description: |-
                  WaitForReadyToUse determines whether the controller should wait for
                  the created VolumeSnapshot to become readyToUse before finishing
                  provisioning. Defaults to false.
                type: boolean
            required:
            - clusterName
            - region
            - volumeSnapshotClassName
            type: object
          status:
            description: status defines the observed state of EBSSnapshotProvision
            properties:
              error:
                description: |-
                  Error contains the error message from the last failed reconciliation.
                  Empty when the last reconciliation succeeded.
                type: string
              lastUpdated:
                description: LastUpdated is the timestamp of the last status update.
                format: date-time
                type: string
              phase:
                description: Phase reflects the high-level state of the last reconciliation.
                type: string
              snapshotsCreatedByNamespace:
                additionalProperties:
                  type: integer
                description: |-
                  SnapshotsCreatedByNamespace is the cumulative number of VolumeSnapshot
                  resources created per namespace across all reconciliations since this
                  resource was created.
                type: object
              snapshotsFound:
                description: |-
                  SnapshotsFound is the total number of AWS snapshots matched for this
                  cluster during the last reconciliation.
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
