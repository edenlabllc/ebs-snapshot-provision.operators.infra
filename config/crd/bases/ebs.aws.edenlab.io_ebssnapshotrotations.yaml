---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: ebssnapshotrotations.ebs.aws.edenlab.io
spec:
  group: ebs.aws.edenlab.io
  names:
    kind: EBSSnapshotRotation
    listKind: EBSSnapshotRotationList
    plural: ebssnapshotrotations
    singular: ebssnapshotrotation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: PHASE
      type: string
    - jsonPath: .status.lastUpdated
      name: LAST-UPDATED
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EBSSnapshotRotation is the Schema for the ebssnapshotrotations
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of EBSSnapshotRotation
            properties:
              frequency:
                description: Frequency sets the safety-net resync interval after all
                  targets complete.
                type: string
              snapshotRotations:
                additionalProperties:
                  description: |-
                    SnapshotRotationTarget describes one independent rotation unit: a set of
                    PVCs to snapshot and the retention policy to apply afterwards. Each key
                    under EBSSnapshotRotationSpec.SnapshotRotations is executed and tracked
                    independently — adding a new key to an existing resource runs only that
                    new target, without re-running targets that already completed.
                  properties:
                    namespace:
                      description: Namespace is the namespace to look up PersistentVolumeClaims
                        in.
                      type: string
                    pvcSelector:
                      description: |-
                        PVCSelector selects which PersistentVolumeClaims in Namespace to
                        snapshot. If empty, all PVCs in Namespace are selected.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    retention:
                      description: |-
                        Retention defines how many snapshots to keep per PVC after this
                        target runs.
                      properties:
                        maxCount:
                          description: |-
                            MaxCount is the maximum number of VolumeSnapshot resources to keep per
                            PVC after this rotation runs. Snapshots exceeding this count are
                            deleted, oldest first, based on their creation timestamp.
                          minimum: 1
                          type: integer
                        purgePolicy:
                          default: Deferred
                          description: |-
                            PurgePolicy controls how snapshots exceeding MaxCount are removed.
                            Defaults to Deferred.
                          enum:
                          - Deferred
                          - Immediate
                          type: string
                      required:
                      - maxCount
                      type: object
                    volumeSnapshotClassName:
                      description: |-
                        VolumeSnapshotClassName is the VolumeSnapshotClass used when creating
                        VolumeSnapshot resources.
                      type: string
                  required:
                  - namespace
                  - retention
                  - volumeSnapshotClassName
                  type: object
                description: |-
                  SnapshotRotations maps an arbitrary target name to its rotation
                  configuration. Each key is executed and tracked independently.
                minProperties: 1
                type: object
            required:
            - snapshotRotations
            type: object
          status:
            description: status defines the observed state of EBSSnapshotRotation
            properties:
              lastUpdated:
                description: LastUpdated is the timestamp of the last status update.
                format: date-time
                type: string
              phase:
                description: RotationPhase represents the high-level state of a single
                  rotation target.
                enum:
                - Pending
                - Completed
                - Failed
                type: string
              targets:
                additionalProperties:
                  description: |-
                    SnapshotRotationTargetStatus is the observed state of a single rotation
                    target, keyed by the same name as in Spec.SnapshotRotations.
                  properties:
                    completionTime:
                      description: CompletionTime is the timestamp when this target
                        finished successfully.
                      format: date-time
                      type: string
                    error:
                      description: Error contains the error message if the rotation
                        failed for this target.
                      type: string
                    phase:
                      description: |-
                        Phase reflects whether this target's one-shot rotation has run yet,
                        and its outcome.
                      enum:
                      - Pending
                      - Completed
                      - Failed
                      type: string
                    snapshotsCreated:
                      description: |-
                        SnapshotsCreated lists the names of VolumeSnapshot resources created
                        for this target, one per matched PVC.
                      items:
                        type: string
                      type: array
                    snapshotsDeleted:
                      description: |-
                        SnapshotsDeleted lists the names of VolumeSnapshot resources deleted
                        for this target to satisfy Retention.MaxCount.
                      items:
                        type: string
                      type: array
                  type: object
                description: |-
                  Targets holds the observed state of each rotation target, keyed by the
                  same name as in Spec.SnapshotRotations.
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
